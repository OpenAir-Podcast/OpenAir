import 'package:hive_ce/hive.dart';

// This file triggers the generation of the Hive Registrar.
// We keep the list empty to avoid conflicts with @HiveType adapters in model files.
@GenerateAdapters([])
class HiveAdapters {}
